# Lotus Risk Codebase Review Ledger

This ledger tracks the enterprise refactor continuation from
`feat/enterprise-risk-refactor-continuation`.

| Review ID | Scope / pattern | Status | Findings and consequence | Action / follow-up | Evidence |
| --- | --- | --- | --- | --- | --- |
| RISK-REF-001 | Quality measurement and CI truthfulness | Reviewing | The generated `baseline_report.md` described current state as the initial baseline, mixed source and test hotspots, and the quality-gate guide described already-active gates as report-only. This weakened before/after evidence credibility. | Correct generated terminology and gate posture; preserve commit `3254774` as immutable initial baseline; add generator regression tests next. | `make quality-baseline`; initial baseline verified with `git show 3254774:quality/baseline_report.md` |
| RISK-REF-002 | Architecture dependency boundaries | Hardened | Risk calculation orchestration and period metric services depended directly on Prometheus `Histogram`, coupling business calculations to observability infrastructure. | Introduced a narrow metric-duration observer port, moved Prometheus metric ownership into `app.observability`, and added import-linter/static guard coverage prohibiting direct Prometheus or concrete observability imports from service modules outside the sanctioned adapter. | `tests/unit/test_risk_metric_timing_port.py`; `tests/unit/test_service_observability_boundary.py`; focused risk tests; `make architecture-gate`; `python -m mypy --config-file mypy.ini` |
| RISK-REF-003 | Service hotspot modularity | Hardened | Current largest source modules included `risk/period_metrics.py`, `risk/calculation_orchestrator.py`, `risk/helpers.py`, `attribution_engine.py`, `attribution_exposure_history.py`, `attribution_decomposition.py`, `attribution_stateful_inputs.py`, `drawdown_series.py`, `drawdown_periods.py`, `scenario_engine.py`, `rolling_stateful_inputs.py`, `rolling_risk_free_dependency.py`, `rolling_metric_series.py`, and concentration parsing/orchestration modules. `risk/period_metrics.py` mixed non-benchmark period calculation orchestration with benchmark alignment/error-context handling; `risk/calculation_orchestrator.py` mixed metadata/rate orchestration with per-period result construction; `risk/helpers.py` mixed numeric coercion, period wrappers, return transformations, drawdown detail/recovery construction, VaR/expected shortfall, benchmark-relative metrics, and data guards; `attribution_engine.py` mixed response metadata/supportability orchestration with period-window slicing and period-result construction; `attribution_exposure_history.py` mixed position-timeseries fetch orchestration, issuer enrichment, group-key resolution, decimal coercion, and exposure aggregation; `attribution_decomposition.py` mixed attribution-set request/precalculation orchestration with empty-result, contributor, reconciliation, and calculated-result assembly; `attribution_stateful_inputs.py` mixed returns-source request/parse, active benchmark exposure sourcing/alignment, group validation, exposure-history orchestration, and final stateless input assembly; `drawdown_series.py` mixed drawdown summary/risk statistics with episode segment construction and duration logic; `drawdown_periods.py` mixed period-frame construction with result assembly, relative benchmark invocation, episode DTO mapping, and underwater-series inclusion policy; `scenario_engine.py` mixed static CIO scenario-pack catalog data with request-context assembly, scenario evaluation, policy-breach supportability, and response assembly; `stateless_resolver.py` mixed caller/core issuer enrichment with weighted stateless state and final computation payload assembly; `concentration/response_builder.py` mixed concentration metric derivation with response DTO construction and supportability recording; `rolling_stateful_inputs.py` mixed dependency selection/reporting-currency resolution, source request/fetch behavior, parsing, and final assembly; `rolling_risk_free_dependency.py` mixed dependency resolution with missing-risk-free coverage probing and detail mapping; `rolling_metric_series.py` mixed rolling maximum-drawdown series math and non-benchmark rolling metrics with the rolling metric dispatcher; `rolling_engine.py` mixed response orchestration with per-period result construction; concentration parsing mixed snapshot display-name enrichment, position parsing, and valuation-context extraction; `concentration/resolvers.py` mixed stateful orchestration with lotus-core snapshot payload/fetch/baseline extraction; and simulation resolution mixed session orchestration with snapshot-state assembly. | Extracted benchmark-dependent period metric alignment, error handling, and context construction into `risk/benchmark_period_metrics.py`; extracted risk period result construction into `risk/period_results.py`; extracted risk benchmark-relative metric calculations into `risk/benchmark_metrics.py`; extracted numeric coercion into `risk/numeric.py`; extracted risk drawdown detail and recovery construction into `risk/drawdown_details.py`; extracted attribution period-window and period-result construction into `attribution_period_results.py`; extracted attribution exposure grouping and aggregation into `attribution_exposure_points.py`; extracted attribution-set result assembly into `attribution_set_results.py`; extracted attribution stateful returns sourcing into `attribution_stateful_returns.py`; extracted attribution active benchmark exposure sourcing/alignment into `attribution_active_benchmark_exposure.py`; extracted drawdown episode records, duration calculation, segment extraction, and extreme-field selection into `drawdown_episodes.py`; extracted drawdown input frame and period series preparation into `drawdown_period_series.py`; extracted static CIO scenario-pack definitions and supported bucket vocabulary into `scenario_pack_catalog.py`; extracted stateless concentration caller/core issuer enrichment into `concentration/stateless_issuer_mapping.py`; extracted concentration snapshot display-name enrichment into `concentration/snapshot_display_names.py`; extracted concentration stateful snapshot payload/fetch/baseline extraction into `concentration/stateful_snapshot.py`; extracted concentration response metric derivation into `concentration/response_metrics.py`; extracted rolling dependency selection and reporting-currency resolution into `rolling_stateful_dependency_selection.py`; extracted rolling stateful source request/fetch behavior into `rolling_stateful_source_responses.py`; extracted rolling risk-free coverage probing and detail mapping into `rolling_risk_free_coverage.py`; extracted rolling benchmark metric-series calculations into `rolling_benchmark_metric_series.py`; extracted rolling maximum-drawdown series math into `rolling_max_drawdown_series.py`; extracted rolling period result construction into `rolling_period_results.py`; extracted concentration issuer mapping/enrichment logic into `concentration/issuer_mapping.py`; extracted concentration simulation snapshot-state assembly into `concentration/simulation_snapshot.py`. Continue reviewing remaining concentration parsing/resolver hotspots. | `tests/unit/test_risk_engine.py`; `tests/unit/test_risk_engine_branch_coverage.py`; `tests/unit/test_risk_mode_adapter_characterization.py`; `tests/integration/test_risk_calculate.py`; scenario focused tests; drawdown focused tests; rolling focused tests; attribution focused tests; concentration focused tests; `python -m mypy --config-file mypy.ini`; touched radon MI/CC checks |
| RISK-REF-004 | Security and sensitive-data controls | Hardened | Correlation middleware reflected/logged unbounded caller correlation IDs and preserved malformed or mismatched trace context. Upstream classifiers exposed raw downstream response and transport exception text. Early rejection responses skipped policy headers, and sensitive API responses lacked explicit no-cache/nosniff controls. | Added bounded correlation identity, strict W3C trace-context validation, bounded client-facing upstream messages, consistent enterprise/security response headers, and negative tests. Continue reviewing operational diagnostic redaction. | `tests/unit/test_correlation_middleware.py`; `tests/unit/test_upstream_errors.py`; `tests/unit/test_enterprise_readiness.py`; `quality/security_findings.md`; `make security-audit` |
| RISK-REF-005 | OpenAPI and error governance | Hardened | Operation metadata is actively gated; the Lotus error envelope previously lacked problem-details-compatible fields, standard error examples were repeated as large nested dictionaries, route-level request examples were concentrated in one large facade module, and runtime exception handlers lived beside static OpenAPI error-response metadata. | Added additive RFC 7807/problem-details metadata inside the existing `error` object, preserving `error.code`, `error.message`, `error.correlation_id`, and `error.details` compatibility; consolidated standard error OpenAPI examples behind small builders; split static route request payloads into the `openapi_request_examples` package while preserving the `openapi_examples.py` facade; split static error-response examples into `api_error_examples.py` while preserving the `app.api_errors.STANDARD_ERROR_RESPONSES` facade used by routers. Continue regenerating final OpenAPI artifact evidence before PR. | `tests/unit/test_error_response.py`; `tests/unit/test_main_error_handlers.py`; `tests/unit/test_openapi_request_examples.py`; `tests/integration/test_health.py`; `make openapi-gate`; `make openapi-artifact-gate` |
| RISK-REF-006 | Observability and operational diagnostics | Hardened | Request middleware emitted an interpolated text log, weakening production parsing and field-level sensitive-data proof. | Replaced it with a bounded structured `request_observation` event and tests proving query strings and bodies are excluded. Continue reviewing operator diagnostics and production threshold evidence. | `tests/unit/test_correlation_middleware.py`; `make observability-contract-validate`; observability tests |
| RISK-REF-007 | Skills, guidance, documentation, and agent context | Hardened | Long-running refactor guidance did not require a recurring conscious review of agent effectiveness artifacts. | Added a five-slice/major-decision review cadence and durable register. Existing backend-delivery and review-ledger skills remain sufficient; repository context remains accurate. | `quality/agent_effectiveness_review.md`; `tests/unit/test_agent_effectiveness_review.py` |
| RISK-REF-008 | Source module size regression | Signed Off | Current source modularization keeps the largest Python source file at 401 lines, but CI only reported size and could allow new monoliths. | Added an active 450-line source-file regression gate to local `check`/`ci` and all GitHub delivery lanes, with focused tests. | `make source-size-gate`; `tests/unit/test_source_size_gate.py`; workflow lint |
| RISK-REF-009 | Downstream connection lifecycle | Hardened | Both downstream profiles configured pool limits, but each operation constructed and closed a new HTTP client, preventing cross-request connection reuse and leaving shutdown ownership implicit. Profile resolution also mixed environment parsing with HTTP-client construction and request execution/error observation; lotus-performance async returns polling mixed poll orchestration with payload validation and bounded async failure construction. | Added application-lifespan ownership of reusable dependency-specific HTTP pools, explicit draining/close behavior, and compatibility-preserving injected/standalone paths. Extracted timeout, pool, keepalive, and async polling environment parsing into `downstream_profile_env.py`; extracted downstream request execution, upstream error normalization, and upstream request observation into `downstream_request_execution.py`; extracted lotus-performance async accepted/result payload validation and failure/timeout error construction into `performance_returns_payloads.py`. Continue reviewing bounded retry posture separately because mutation retries require operation-specific idempotency guarantees. | `tests/unit/test_app_lifecycle.py`; downstream client tests; lotus-performance client tests; health tests; `python -m mypy --config-file mypy.ini` |
| RISK-REF-010 | Runtime configuration and downstream URL safety | Hardened | Core and performance URL resolution duplicated permissive handling; malformed or credential-bearing values failed late, and the required configuration guide was missing. | Added a shared safe URL resolver, negative tests, complete runtime configuration guide, expanded environment example, and security/runbook evidence. | `tests/unit/test_downstream_base_url.py`; `tests/unit/test_configuration_docs.py`; downstream client tests; `make security-audit` |
| RISK-REF-011 | Enterprise bank runtime enforcement | Hardened | Documented bank mode required authorization and explicit settings, but enabling runtime enforcement did not require that posture and could start permissively. | Made enterprise runtime enforcement fail application construction with bounded issue codes unless the in-process bank posture is complete; kept local defaults unchanged. | `tests/unit/test_enterprise_readiness.py`; deployment-policy and configuration-doc tests; `make security-audit` |
| RISK-REF-012 | Enterprise authorization module boundary | Hardened | `enterprise_readiness.py` mixed capability-rule parsing, write authorization, runtime validation, redaction, audit emission, policy metadata, payload limits, and middleware assembly, making the security boundary harder to inspect. | Extracted capability-rule parsing and write authorization into `enterprise_authorization.py`; extracted audit emission and sensitive metadata redaction into `enterprise_audit.py`; extracted policy-version lookup into `enterprise_policy.py`; preserved compatibility exports from `enterprise_readiness.py`. | `tests/unit/test_enterprise_readiness.py`; app factory/configuration/deployment-policy/security-evidence tests; `python -m mypy --config-file mypy.ini`; touched radon MI/CC checks |
| RISK-REF-013 | Repo hygiene and CI lane parity | Hardened | Generated artifacts and cache byproducts were ignored but cleanup was an inline narrow command, while `make ci` and GitHub lanes did not fully align with the governed PR/main gate contract. This could leave stale evidence and create avoidable post-push CI failures. Remote Feature Lane then proved the mesh contract gate also needed its platform-contract checkout in GitHub, scoped after repo-root scanners. | Added a tested cleanup utility, wired `make clean`, expanded ignored cache policy, made `make ci` the PR-grade local aggregate, clarified `ci-local` as a partial split-suite loop, wired mesh-contract validation into Feature, PR Merge, and Main Releasability lanes, and added post-scanner `.lotus-platform` checkout/root resolution for remote contract validators. | `tests/unit/scripts/test_clean_generated_artifacts.py`; `tests/unit/test_ci_gate_contract.py`; `tests/unit/test_domain_data_product_contract_check.py`; `git branch -r --no-merged origin/main`; wiki check-only `DiffCount 0`; remote runs `28706194048`, `28706511710`, `28706568526` |
| RISK-REF-014 | Upstream failure boundary reuse | Hardened | GitHub issues #157, #162, #179, and #181 exposed the same boundary weakness: operation-specific upstream states, malformed upstream payload fields, and raw upstream messages were handled too close to individual call sites. This made async polling, stateful return parsing, concentration snapshot/session orchestration, and public error envelopes easy to regress independently. | Added operation-owned accepted-status support to shared downstream request execution, scoped async result `404`/`202` pending handling to the lotus-performance result endpoint, mapped malformed upstream return dates to `UPSTREAM_INVALID_RESPONSE` in the shared stateful return parser, bounded public upstream messages at the FastAPI exception handler, and added a concentration-specific upstream contract helper so malformed lotus-core stateful snapshot, simulation snapshot, and create-session payloads return dependency-coded `UPSTREAM_INVALID_RESPONSE` envelopes with safe service, operation, category, retryability, snapshot-mode, and reason metadata. No runtime split is justified; this is a design-boundary hardening slice inside the existing service. | `tests/unit/test_downstream_client_profile.py`; `tests/unit/test_lotus_performance_client.py`; `tests/unit/test_stateful_returns_series_parser.py`; `tests/unit/test_main_error_handlers.py`; `tests/integration/test_drawdown_endpoint.py`; `tests/unit/test_concentration_engine_modes.py`; `tests/integration/test_concentration_lotus_core_characterization.py` |
| RISK-REF-015 | Enterprise authorization route-contract parity | Hardened | GitHub issues #164 and #166 showed that enterprise authorization behavior was split across middleware, environment maps, docs, and OpenAPI: startup accepted partial capability maps, and generated consumers could not discover the enterprise caller-context contract. | Added a governed supported write-route inventory, prefix-aware startup coverage validation, generated OpenAPI caller-context extension, and OpenAPI quality-gate enforcement so runtime authorization, published API contract, and deployment docs stay aligned. | `tests/unit/test_enterprise_readiness.py`; `tests/unit/test_openapi_quality_gate.py`; `tests/integration/test_health.py`; `docs/security-deployment-policy.md`; `docs/api-governance.md` |
| RISK-REF-016 | Enterprise downstream runtime-profile validation | Hardened | GitHub issue #185 showed that malformed explicit downstream timeout, pool, keepalive, and async polling overrides were silently replaced by defaults even when enterprise runtime enforcement was enabled. This made runtime resilience posture harder to audit and support. | Added a central downstream runtime-setting validator, wired it into enterprise startup validation, kept local fallback behavior explicit, and documented the bounded `invalid_downstream_runtime_setting:<ENV_NAME>` issue-code contract. | `src/app/integrations/downstream_profile_env.py`; `tests/unit/test_enterprise_readiness.py`; `tests/unit/test_downstream_client_profile.py`; `docs/configuration.md`; `docs/runbooks/service-operations.md` |
| RISK-REF-017 | Enterprise body-limit deployment proof | Hardened | GitHub issue #183 showed that bank-mode docs required ingress/proxy and ASGI/server body limits, but startup only validated the in-process `ENTERPRISE_MAX_WRITE_PAYLOAD_BYTES` setting. This let direct local Uvicorn/Compose posture look enterprise-ready without machine-readable external body-limit evidence. | Added enterprise startup validation for `ENTERPRISE_INGRESS_MAX_BODY_BYTES` and `ENTERPRISE_ASGI_MAX_BODY_BYTES`, bounded missing/invalid/oversized proof issue codes, local-only direct runtime documentation, and wiki/security/runbook evidence. | `src/app/enterprise_readiness.py`; `tests/unit/test_enterprise_readiness.py`; `docs/security-deployment-policy.md`; `docs/security-threat-model.md`; `wiki/Security-and-Governance.md` |
| RISK-REF-018 | Trusted ingress and protected operator diagnostics | Hardened | GitHub issues #161 and #163 showed that caller context headers and operator endpoints relied on implicit gateway/network assumptions. Direct clients could present actor/service/capability headers, and `/ops`, `/ops/trust-telemetry`, and `/metrics` were not protected by executable enterprise proof. | Added a dedicated trusted-ingress helper, enterprise startup validation for `ENTERPRISE_TRUSTED_INGRESS_SECRET`, middleware enforcement for writes and protected operator endpoints, health-probe compatibility, and docs/wiki evidence that gateway/ingress must strip caller-supplied trusted markers before injecting its own. | `src/app/enterprise_trusted_ingress.py`; `tests/unit/test_enterprise_readiness.py`; `docs/security-deployment-policy.md`; `docs/security-threat-model.md`; `wiki/Security-and-Governance.md` |
| RISK-REF-019 | Typed runtime downstream composition | Hardened | GitHub issue #180 showed that stateful routers used raw `Request` objects and a request-time resolver that could silently construct concrete downstream clients when lifespan state was absent. That spread runtime composition across routers, app state, test monkeypatches, and concrete fallback constructors. | Added `src/app/runtime/downstream_clients.py` as the typed composition boundary, removed the old fallback resolver, moved routers to typed runtime and caller-context dependencies, registered structured `RUNTIME_COMPOSITION_ERROR` handling, converted tests to runtime overrides, and tightened the router architecture guard. | `src/app/runtime/downstream_clients.py`; `src/app/routers/*.py`; `.importlinter`; `tests/unit/test_runtime_downstream_clients.py`; `tests/unit/test_runtime_composition_boundaries.py`; `tests/integration/test_risk_calculate.py`; `make architecture-gate` |
| RISK-REF-020 | Readiness dependency posture truth | Hardened | GitHub issue #160 showed that `/health/ready`, `/ops`, and trust telemetry described configured dependency URLs as dependency-aware readiness, even though no active probes or periodic dependency-status updater existed. That let a configured but unreachable upstream look operationally ready by default. | Narrowed the contract to configured-only dependency posture by default with `status="configured"` and `detail="configured_only_no_probe"`, preserved degraded/unavailable override behavior, added tests for unprobed unreachable URLs, and updated API docs, runbooks, wiki source, API vocabulary, and repo context to prevent live-reachability overclaims. No runtime probe was added because this slice deliberately reduces runtime complexity while preserving endpoint-level downstream failure supportability. | `src/app/ops_runtime.py`; `src/app/contracts/ops.py`; `tests/integration/test_health.py`; `tests/unit/test_trust_telemetry.py`; `docs/domain-apis/operational-endpoints.md`; `wiki/Operations-Runbook.md` |
| RISK-REF-021 | Service observability port boundary | Hardened | GitHub issue #167 showed that service modules no longer imported Prometheus directly but still imported concrete `app.observability` functions, leaving business/supportability helpers coupled to the infrastructure adapter. | Added `src/app/services/observability_ports.py` as the documented service-facing adapter, moved risk metric requests/duration, supportability/freshness, and endpoint execution recording behind it, and added `.importlinter` plus AST guard tests so only that adapter can import `app.observability`. Existing metric labels and Prometheus behavior are preserved. | `src/app/services/observability_ports.py`; `.importlinter`; `tests/unit/test_service_observability_boundary.py`; `make architecture-gate`; `tests/unit/test_calculation_supportability.py` |
| RISK-REF-022 | API DTO to calculation-domain boundary | Hardened | GitHub issue #182 showed that pure calculation helpers used public Pydantic response DTOs as internal calculation values, making API response shape changes riskier than necessary. | Classified `app.contracts` models in architecture docs and migrated the concentration driver representative path: math helpers now return internal `TopPositionDriverValue` and `TopIssuerDriverValue` dataclasses, while `response_builder.py` maps those values to the public `TopPositionDriver` and `TopIssuerDriver` DTOs. This preserves response compatibility while establishing the migration pattern for broader DTO/domain separation. | `src/app/services/concentration/math.py`; `src/app/services/concentration/datamodels.py`; `src/app/services/concentration/response_builder.py`; `tests/unit/test_concentration_domain_boundary.py`; concentration compatibility tests |
| RISK-REF-023 | Concentration simulation projected-state semantics | Hardened | GitHub issue #178 showed that simulation mode reused current concentration values whenever `positions_projected` extracted to an empty list, collapsing missing projected source data, explicit empty projected books, and non-empty projected books into one truthiness fallback. This could report no concentration change after a full-liquidation simulation or source projected-state failure. | Added explicit proposed-empty fallback policy to the computation input, disabled fallback for simulation mode, required simulation snapshots to include a list-valued `positions_projected` section, preserved explicit empty projected lists as empty proposed books with zero proposed concentration metrics, and updated OpenAPI descriptions plus concentration methodology docs. No runtime split or repo-context expansion is justified; the durable truth lives in API/methodology docs and tests. | `src/app/services/concentration/simulation_snapshot.py`; `src/app/services/concentration/response_metrics.py`; `tests/unit/test_concentration_engine_modes.py`; `tests/integration/test_concentration_lotus_core_characterization.py`; `tests/unit/test_methodology_docs.py`; concentration methodology docs |
